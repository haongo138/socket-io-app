export const DEFAULT_ROOM = "VIP";

export enum Event {
  CONNECTION = "connection",
  JOIN_SERVER = "join_server",
  SEND_MSG = "send_message",
  RECEIVE_MSG = "receive_message",
  DISCONNECT = "disconnect",
}